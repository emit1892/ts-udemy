let message: string = "Hello, ts-node-dev!";

console.log({ message });

// node src/install-typescript.js
// npx ts-node src/install-typescript.ts コンパイルからnodeでの実行をやってくれるts-node
// npm run dev src/install-typescript.ts ts-node-devを利用し変更されるごとにコンパイルと実行がされる